# GRM Music Repository

A simple repository of Music Contracts and Distribution Methods.
The repository can be searched by some simple search terms returning a formatted result string.

# To Run

The repository is implemented using Visual Studio 2017.

The solution grmtest/grmtest.sln contains a class library grm and unit tests grmunittest
Run the tests to verify the 3 unit tests supplied.

